apiVersion: v1
kind: ReplicationController
metadata:
  name: titofe
  labels:
    name: tito-fe
    version: "latest"
    app: tito
    tiers: tito-fe
spec:
  replicas: 1
  selector:
    type: tito-fe
  template:
    metadata:
      labels:
        app: tito
        type: tito-fe
    spec:
      containers:
        - name: tito
          image: vmeoc/tito-fe:latest
          ports:
          - containerPort: 80
          env:
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace

