apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tito-test
#  annotations:
#    ncp/rewrite_target: "/"
  labels:
    app: tito
    stage: dev
spec:
  rules:
   - host: tito-test.cpod-gv.shwrfr.com #set your ingress URL here
     http:
       paths:
       - path: /tito/*
         backend:
           serviceName: titofe-service
           servicePort: 80
