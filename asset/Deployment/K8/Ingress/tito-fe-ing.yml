apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tito-prod
#  annotations:
#    ncp/rewrite_target: "/"
  labels:
    app: tito
    stage: dev
spec:
  rules:
   - host: tito-prod.cpod-gv.shwrfr.com #set your ingress URL here
     http:
       paths:
       - path: /tito/*
         backend:
           serviceName: titofe-service
           servicePort: 80
